config UART_PPU
        bool "UART driver for PPU"
        depends on UART
        default n
        help
          enable UART support for PPU

config UART_PPU_INTERRUPT
        bool "PPU UART Interrupt Support"
        depends on UART_PPU
        default n
        help
          Enable Interrupt Support forthe UART Driver.
